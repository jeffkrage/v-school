// function calculateAge(birthYear) {
//     return 2018 - birthYear;
// }



// // const h1 = document.createElement('h1')
// // h1.innerHTML = year
// // document.getElementById('javascript-output').appendChild(h1)
// let ageJohn = calculateAge (1990);  // Age John will be the result of calling that function and passing in the argument of 1990 to get the result of 28.
// let ageMike = calculateAge (1948);
// let ageJane = calculateAge (1969);

// console.log(ageJohn, ageMike, ageJane);

// function yearsUntilRetirement(year, firstName) {
//     let age = calculateAge(year);
//     let retirement = 65 - age;
//     console.log(firstName +  " retires in " + retirement + ' years');
// }
// yearsUntilRetirement( 1990, 'John')

function test() {   /// Declared Function.
    
};console.log(test());

// parameters are used when defining a function, they are the names created in the function definition. 
// Arguments are the VALUES the function receives from each parameter when a function is invoked.
let double = function (num) {  // Expressed Function.
    return num * 2;
}
console.log(4);